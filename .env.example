# Painel de Base Eleitoral - Renilce e Nene

## Variáveis de Ambiente
- `GEMINI_API_KEY`: Chave para integração com IA (opcional).
- `APP_URL`: URL base da aplicação.
- `DATABASE_URL`: Caminho para o banco SQLite (padrão: ./database.sqlite).
